// tipos primitivos 
// string, number, undefined, null, boolean, symbol(não tratado aqui)
 
const nome =  'David' // string
const nome1 = 'David' // string
const nome2 = 'David' // string
const num1 = 10 // number
const num2 = 15 // number
const condicao = true  //booleano
const aprovado = false // booleano
const sobreNome = null // Nulo = não aponta para local nenhum na memória 

const a = [1, 
           2
          ]
const b = a
b.push(3)
console.log(typeof aprovado);
console.log(a, b);